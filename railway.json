{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build -w apps/web && npm run db:generate -w apps/api && npm run build -w apps/api"
  },
  "deploy": {
    "startCommand": "npm run db:migrate:prod -w apps/api && npm run db:seed -w apps/api && npm run start -w apps/api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}

