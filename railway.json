{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run db:generate -w apps/api && npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:migrate:prod -w apps/api && npm run db:seed -w apps/api && npm run start -w apps/api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
